{
  "name": "devunity-codearena",
  "version": "1.0.0",
  "private": true,
  "description": "DEVUNITY CODEARENA — Compete. Build. Improve. Belong.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix web run build",
    "seed": "node server/src/db/seed.js",
    "start": "node server/src/index.js",
    "dev": "node scripts/dev.mjs",
    "smoke": "node scripts/smoke.mjs",
    "uitest": "node scripts/uitest.mjs",
    "test": "npm run smoke"
  }
}
